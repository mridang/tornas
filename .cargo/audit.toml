[advisories]
# Advisory ignores with rationale go here; the audit workflow reads this file.
ignore = [
    # ringbuf < 0.5.2: double free in Consumer::skip/clear when an element's Drop
    # panics. Only librqbit-utp uses it, as SharedRb<Heap<u8>>; u8 has no Drop, so
    # the bug cannot trigger. Fixed in ringbuf 0.5, which needs a librqbit-utp release.
    "RUSTSEC-2026-0293",
]
