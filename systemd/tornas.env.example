# /etc/tornas/tornas.env  (read by the systemd unit; config.toml holds the rest)
TORNAS_DISK_BUDGET=800G
TORNAS_MIN_FREE=20G
TORNAS_TMDB_TOKEN=
TORNAS_API_TOKEN=
#TORNAS_LOG_DIR=/var/log/tornas
#TORNAS_HTTP_LISTEN=[::]:3030
#TORNAS_PUBLIC_URL=http://tornas.local:3030
