[tornas]
livingroom-pi  ansible_host=192.168.1.20
nas            ansible_host=192.168.1.30  tornas_disk_budget=3T  tornas_min_free=100G

[tornas:vars]
ansible_user=pi
# Secrets belong in an encrypted group_vars file, not here:
#   ansible-vault create group_vars/tornas/vault.yml
#   tornas_tmdb_token: "..."
#   tornas_api_token: "..."
