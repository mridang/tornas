---
# Restart, not reload: `systemctl reload tornas` only refreshes the tracker list
# and does not re-read these files. State is flushed on SIGTERM, but streams in
# progress are dropped.
- name: Restart tornas
  ansible.builtin.systemd_service:
    name: tornas
    state: restarted
    daemon_reload: true
